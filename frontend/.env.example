# Cloudflare Worker URL (search, metadata, thumbnails)
# Leave empty for dev (Vite proxies to localhost:8787)
VITE_API_URL=

# Deno stream service URL
# Leave empty for dev (defaults to localhost:8788)
VITE_STREAM_URL=

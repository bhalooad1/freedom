# ===========================================
# DEVELOPMENT (localhost)
# ===========================================

# API Worker URL (search, metadata, thumbnails)
# Leave empty for dev - defaults to localhost:8787
VITE_API_URL=

# Stream Worker URL (video streaming)
# Leave empty for dev - defaults to localhost:8789
VITE_STREAM_WORKER_URL=

# Stream backend (always 'worker' now)
VITE_STREAM_BACKEND=worker

# ===========================================
# PRODUCTION EXAMPLE
# ===========================================
# VITE_API_URL=https://freedom-api.YOUR-SUBDOMAIN.workers.dev
# VITE_STREAM_WORKER_URL=https://freedom-stream.YOUR-SUBDOMAIN.workers.dev
# VITE_STREAM_BACKEND=worker

# Freedom Stream Service Configuration
# Change secret_key to any 16 alphanumeric characters

[server]
# PORT env var overrides this in production
port = 8788
# Use 0.0.0.0 to accept external connections
host = "0.0.0.0"
secret_key = "freedomstream123"
base_path = "/"

[cache]
enabled = true

[jobs.youtube_session]
po_token_enabled = true
